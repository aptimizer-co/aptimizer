import { Container } from "@/src/components";
import s from "./VisionSection.module.css";

const VisionSection = () => {
  return (
    <section className={s.VisionSection}>
      <Container>
        <p>Excellence for all</p>
      </Container>
    </section>
  );
};

export default VisionSection;
